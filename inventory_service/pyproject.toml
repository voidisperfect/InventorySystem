[project]
name = "inventory-service"
version = "0.1.0"
description = "Django Service for Inventory Management"
dependencies = [
    "django>=5.0",
    "djangorestframework",
    "django-environ",
    "faststream[kafka]",
    "psycopg[binary]",     
    "loguru>=0.7.0",
    "gunicorn>=21.2.0",
]